import { FarmerLoginDetails } from './farmer-login-details';

describe('FarmerLoginDetails', () => {
  it('should create an instance', () => {
    expect(new FarmerLoginDetails()).toBeTruthy();
  });
});
