import { FarmerBankDetails } from './farmer-bank-details';

describe('FarmerBankDetails', () => {
  it('should create an instance', () => {
    expect(new FarmerBankDetails()).toBeTruthy();
  });
});
