import { BidderLoginDetails } from './bidder-login-details';

describe('BidderLoginDetails', () => {
  it('should create an instance', () => {
    expect(new BidderLoginDetails()).toBeTruthy();
  });
});
