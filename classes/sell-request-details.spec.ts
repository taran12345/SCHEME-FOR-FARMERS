import { SellRequestDetails } from './sell-request-details';

describe('SellRequestDetails', () => {
  it('should create an instance', () => {
    expect(new SellRequestDetails()).toBeTruthy();
  });
});
